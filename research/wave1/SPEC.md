# Wave-1 사전등록 스펙 — Bitget 실행 가능 전략 탐색 (동결: 2026-07-15)

## 목표·제약
- 자본 $300+, 실행 거래소 Bitget(USDT-M 선물 + 캐리 현물 레그), 리서치 데이터 Binance(깊이) + Bitget(실행 캘리브).
- "안정적 수익극대화" 조작적 정의: OOS 비용후 +EV, MC 5%분위 최종자본 > 초기, MDD ≤ 25%, 파산확률(자본 50% 소실) < 5%. 이 게이트를 통과하는 후보 중 Calmar 최대.
- 배포 판단은 19게이트(`~/.claude/references/strategy_validation_checklist.md`) 강조 게이트 2·3·4·5·7·9·16 전부 PASS 필요. 미통과 = "엣지 없음" 정직 보고 후 wave-2.
- 이 wave에서 시험하는 후보는 아래 15개가 전부다. 사후 추가 금지(추가 = wave-2 신규 등록).

## 데이터 (게이트 1)
| 소스 | 용도 | 범위 |
|---|---|---|
| Binance fapi klines 1D/4H | F1·F2 가격 | 심볼 상장~2026-07-14 (BTC 퍼프 2019-09~) |
| Binance spot klines 1D | F1 현물 레그 | 동일 |
| Binance fapi fundingRate | F1·F2 펀딩 | 2019-09~ (8h) |
| Bitget v2 mix candles 1H/1D | F3 주식선물 + 교차검증 | SPY 2026-02~, TSLA 2025-08~, BTC 2023-04~ |
| Bitget history-fund-rate | 펀딩 교차검증(133d) + F3-H3 | 최근 133d |
| Yahoo chart API 일봉 (SPY, QQQ) | F3-H1 기저효과 | ~20년 (Stooq는 이 네트워크에서 봇검증 차단 → 치환 2026-07-15, 가설 불변) |

무결성 체크(전 소스): 타임스탬프 단조·중복 0·갭 스캔(허용 갭 리포트)·거래량>0 비율·Binance↔Bitget 종가 상관 >0.99(겹침 구간). 실패 시 해당 심볼 제외 기록.

## 비용 모델 (게이트 7 — 전 후보 공통 적용)
- 퍼프 테이커 0.06%/사이드, 현물 테이커 0.10%/사이드 (Bitget 실측 수수료; 보수적으로 전부 테이커 가정).
- 슬리피지 = 하프스프레드: BTC/ETH/SOL 1bp, 기타 알트 3bp, 주식선물 5bp (실측 0.02~3.5bp 대비 보수).
- 펀딩: 포지션 보유 중 8h 경계마다 실제 히스토리 부과. 부호 규약: funding>0 → 롱이 숏에 지불.
- 스트레스 변형: 슬리피지 ×2 재실행 (게이트 7 민감도).

## 체결 규약 (룩어헤드 차단)
- 시그널은 바 t 종가로 계산 → 바 t+1 시가 체결. 동일 바 체결 금지.
- 스탑은 바 내 터치 시 스탑가 체결(보수: 갭이면 시가).

## 분할 (게이트 3·4)
- IS: 상장~2025-09-30. OOS 홀드아웃: 2025-10-01~2026-07-14 (동결, 후보당 1회만 평가).
- 파라미터는 전부 사전동결(피팅 없음) → 게이트 2는 이웃 파라미터 민감도 + 게이트 3은 연도별 서브피리어드 일관성 표로 판정.

## 후보 15개 (동결)

### F1 펀딩 캐리 로테이션 — 6개
유니버스: Binance UM 퍼프 중 펀딩 히스토리 ≥24mo ∧ Bitget 상장 ∧ 현물 존재, 현 시점 quote volume 상위 40.
포지션: 숏 퍼프 + 롱 현물 동노셔널(델타중립), 두 레그 실가격 PnL + 펀딩 수취 − 비용.
score(t) = 최근 W 펀딩 평균 연율화. 진입 score>θ, 청산 score<θ/2. 일봉 리밸런스, 상위 K개 균등.
| ID | W | θ(APR) | K |
|---|---|---|---|
| F1a | 3d | 8% | 2 |
| F1b | 3d | 15% | 2 |
| F1c | 7d | 8% | 2 |
| F1d | 7d | 15% | 2 |
| F1e | 7d | 8% | BTC/ETH만 |
| F1f | 7d | 15% | 4 |

### F2 TSMOM (BTC·ETH·SOL 1D, 3자산 균등 포트폴리오) — 6개
공통 오버레이: 변동성 타게팅(일 1.5% 목표, realized vol 20d), 레버리지 캡 3x, 보유 중 실제 펀딩 부과.
| ID | 규칙 | 방향 |
|---|---|---|
| F2a | Donchian 20 돌파, ATR20×3 트레일링 | 롱숏 |
| F2b | Donchian 55 돌파, ATR20×3 트레일링 | 롱숏 |
| F2c | Donchian 20 | 롱온리 |
| F2d | Donchian 55 | 롱온리 |
| F2e | MA50/200 골든크로스 | 롱-플랫 |
| F2f | MA100 기울기 부호 | 롱숏 |

### F3 주식·ETF 토큰선물 세션 효과 — 3개 (탐색 등급: 데이터 5~11mo → wave-1 내 배포 청구 금지)
| ID | 가설 |
|---|---|
| F3-H1 | 오버나이트 드리프트: 기저(Stooq 20년, close→open vs open→close) 확립 → Bitget SPY/QQQ 1H 복제 여부 + 비용후 판정 |
| F3-H2 | 주말 크립토-베타: 미장 휴장 중(금 21:00→월 13:30 UTC) 주식선물 수익률의 BTC 베타 및 드리프트 |
| F3-H3 | 주식퍼프 펀딩 편향: SPY/QQQ/TSLA/NVDA/MSTR 펀딩 133d 평균·t-stat → 캐리 실현성 노트 |

## 사이징 ($300 기준, 게이트 8·9·16)
- 검증된 +EV 후보에만: 0.25×Kelly, 레버리지 캡 2x, 동시 포지션 ≤3, 포지션당 노셔널 ≤ $150, 최소주문 5 USDT 충족 확인.
- 뱅크롤 MC: $300 시작, OOS 트레이드 분포 부트스트랩 1e4 경로 → P(최종<$150) < 5% 필요.

## 산출물
- `research/wave1/cache/` (gitignore) — 원시 데이터
- `research/wave1/results/` — 후보별 트레이드·에쿼티·메트릭 JSON + 게이트 표
- `research/wave1/REGISTRY.md` — 15후보 전수 원장 (다중검정 정직성)
- `research/wave1/report/wave1_report.md` — 최종 verdict
