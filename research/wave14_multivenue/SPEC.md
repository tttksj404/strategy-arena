# Wave-14 사전등록 — 멀티거래소 × 동시포지션 × 자본티어 (동결: 2026-07-22)

## 근거 — 심볼 축은 소진, 남은 건 다른 차원
wave-13이 심볼 폭 곡선을 확정했다: BTC/ETH 11.15% → top30 14.65% → top100 20.30% → **top200 22.01%(정점)** → 동적358 20.56%(하락). **더 넓힐 심볼이 없다.**
미탐색 차원 3개: ①거래소(전 wave가 Binance 단일 소스) ②동시 포지션 수(L4는 여전히 1쌍) ③자본 티어(①②를 여는 열쇠).

## 신규 데이터 (검증 필요)
- Bybit v5 `/v5/market/funding/history` (2024-01~, wave-13 교차검증에서 접근 확인), Bybit 티커·호가
- OKX `/api/v5/public/funding-rate-history` (최근 ~93일 한도 실측 확인 — **장기 백테스트 불가, 현재 기회 스캔용으로만**)
- Hyperliquid `/info` (fundingHistory) — 접근 가능 여부부터 프로브, 불가 시 제외 기록
- 각 거래소 실측 호가 → wave-13 방식(half-spread + $45 book-walk)으로 거래소별 비용 매핑 산출. **거래소별 수수료 실제값 사용**(Bitget 메이커 0.02%, Bybit/OKX 각 공시값 확인).
장기 백테스트 가능 조합은 **Binance+Bybit(2024-01~)** 뿐이다. 이 구간이 짧다는 것(약 2.5년, 고펀딩기 2024 포함)을 리포트에 명시하고, 표본 부족 시 UNDETERMINED 처리.

## 구성 8개 (동결, 사후 추가 금지)
공통 고정: 델타중립 2레그, 1x, 진입 15%APR/청산 7.5%, 일봉, 시그널 t종가→t+1시가, **wave-13 실측비용 매핑 + 거래소별 실측**. 현금버퍼 10%.
| ID | 자본 | 거래소 | 동시 쌍 | 비고 |
|---|---|---|---|---|
| M0 | $100 | Binance/Bitget 단일 | 1 | **L4 재현 = 기준선**(멀티거래소 데이터 구간으로 절단해 공정 비교) |
| M1 | $100 | +Bybit | 1 | 기회 풀 확대만 |
| M2 | $300 | 단일 | 3 (레그 $45) | 동시 포지션 축만 |
| M3 | $300 | +Bybit | 3 | 둘 다 |
| M4 | $1,000 | +Bybit | 10 (레그 $45) | 자본 티어 |
| M5 | $3,000 | +Bybit | 30 (레그 $45) | 상위 티어 — 포화 지점 탐색 |
| M6 | $300 | +Bybit | 3 | **거래소간 펀딩 스프레드**: 동일 심볼을 펀딩 높은 거래소에서 숏퍼프 + 낮은 곳에서 롱퍼프(현물 불요, 양쪽 퍼프) |
| M7 | $1,000 | +Bybit | 10 | M6의 자본 확대판 |

## 게이트 (wave-13 S1~S5 승계 + 신규)
- S1 델타중립·1x / S2 MC 1e4 p05 > 초기자본 ∧ P(<초기×0.5) < 1% / S3 블록셔플 MDD p95 ≤ 10% / S4 실행가능(전 레그 ≥$5, gross ≤ 활성자본) / S5 실측슬리피지 ×3 스트레스 생존
- **S6(신규) 거래소 리스크**: M6/M7 거래소간 구조는 양쪽 거래소에 자본이 분리 예치된다. 한 거래소 전액 손실(출금중단·파산) 시나리오에서 남은 자본이 초기의 50% 이상인지 명시. 이 시나리오는 **확률이 아니라 구조적 노출**로 보고(백테스트로 확률 추정 금지).
- 승격: S1~S6 전부 PASS ∧ 고펀딩기 연환산 > 동일 자본 티어의 단일거래소 기준선

## 필수 산출
- 자본 티어별(x축: $100/$300/$1000/$3000) 연환산 수익률 곡선 — **동시 포지션 증가가 언제 포화하는지**
- 거래소 추가의 순효과: 기회 증가분 vs 비용·운영 복잡도
- M6/M7 거래소간 스프레드 구조의 수익·리스크 분해
- 데이터 구간 한계(2024-01~) 명시, 표본 부족 항목은 UNDETERMINED

## 판정
- 승격 있으면 자본 티어별 최적 구성표를 카드에 기재.
- 개선 없으면 "거래소·포지션 축도 포화, L4가 상한" 정직 보고.
- 다중검정: 누적 시행 84회 기준 DSR 보정 표기. **M6/M7은 신규 구조(양쪽 퍼프)라 기존 캐리와 별도 패밀리로 등록.**
