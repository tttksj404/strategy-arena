# Fable5 Audit Packet: Oracle RaceLens Deployment

## Claim

RaceLens is deployed on a fixed Oracle Chuncheon VM and no longer depends on the Render/temporary tunnel path for the app preview/API smoke surface.

## Target

- OCI instance: `racelens-prod-ubuntu-20260705161421`
- SSH aliases: `g185`, `racelens-oracle`
- SSH target: `ubuntu@168.107.2.218:22`
- HTTPS URL: `https://168-107-2-218.sslip.io`
- Remote path: `/home/ubuntu/strategy-arena`

## Changes

- Replaced stale local `g185` SSH target with `168.107.2.218` and `ubuntu`.
- Updated Oracle recovery config to the new instance OCID/IP/user.
- Deployed Docker Compose stack on Ubuntu with 8G swap, Docker Compose v2, Caddy, app, mobile-web, postgres, collector, and search-loop.
- Stopped old `g185-restored` instance; preserved it as `STOPPED` rather than terminating.
- Added deploy-script fallback/preservation for remote Compose command, SSH port, existing `ORACLE_SITE_ADDRESS`, and `ACME_EMAIL`.
- Reduced Docker/rsync context by excluding mobile `node_modules`, `dist`, `.expo`, runtime/cache/log folders.
- Disabled inherited app healthcheck for worker-only collector/search-loop services.
- Set search-loop default backoff to 30 seconds to prevent tight-loop log/CPU burn when snapshots are absent.

## Evidence

- `python -m pytest tests/test_oracle_deploy_artifacts.py`: 7 passed.
- `bash -n deploy/oracle/deploy.sh deploy/oracle/smoke.sh`: passed.
- `deploy/oracle/smoke.sh https://168-107-2-218.sslip.io`: passed for `/healthz`, `/`, legal pages, `/api/live-decision`, and `/predict`.
- `curl https://168-107-2-218.sslip.io/healthz`: HTTP 200, TLS verify result 0.
- Remote `docker compose ps`: `oracle-app-1` healthy; caddy, postgres, mobile-web, collector, search-loop all Up.
- Remote env readback: `ORACLE_SITE_ADDRESS=168-107-2-218.sslip.io`, `SEARCH_LOOP_INTERVAL_SEC=30`.
- OCI instance list: old `g185-restored` STOPPED, failed temporary Oracle Linux VM TERMINATED, new Ubuntu RaceLens VM RUNNING.

## Residual Audit Questions

- A live, not-yet-settled race window was not available during the final smoke; odds ingestion must be observed during the next active race window.
- Store production should eventually use an owned domain and real support email instead of `sslip.io` and `admin@example.com`.
