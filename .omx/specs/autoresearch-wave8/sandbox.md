# Wave-8 sandbox

- Read-only source data: `research/wave3/cache/`.
- Writable outputs: `research/wave8_alternative/results/` and `research/wave8_alternative/report/`.
- No network fetch, credentials, exchange SDK, live orders, or deployment changes.
- Parameters are fixed in `SPEC.md` before the run.
